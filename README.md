# Arduino Lab
This is a laboratory for testing and study Arduino. On the main branch we just have a example file that prints a string plus a counter incremented by each second.

## Examples Branches
The examples of this repository will be organized in separate branches, check in the list below the Arduino examples:

- Display LCD (Basic)
